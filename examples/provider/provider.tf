terraform {
  required_providers {
    epilot-opportunity = {
      source  = "epilot-dev/epilot-opportunity"
      version = "0.5.1"
    }
  }
}

provider "epilot-opportunity" {
  # Configuration options
}